# ТЗ: перестройка MOTORA MVP → MOTORHOF CRM

**Для исполнителя (Claude Opus 4.8 / любая модель).** Это управляющий документ. Полные требования заказчика — в `docs/requirements-motorhof.md` (далее «Требования») — читать раздел фазы ПЕРЕД её началом. Конвенции кода — в `CLAUDE.md`, они обязательны. История проекта — в `DEVPLAN.md` (не редактировать, он закрыт; прогресс отмечать здесь).

## Железные правила (нарушение любого = фаза не принята)

1. **Одна фаза за раз.** Не начинать следующую без явной команды пользователя. Фаза заканчивается: рабочим состоянием, прогнанными воротами качества, деплоем на прод, отчётом пользователю.
2. **Ворота качества каждой фазы:** `npx tsc --noEmit && npm run lint && npm run build && npx vitest run` (с фазы 1) — всё чисто. Плюс ручная проверка затронутых сценариев в браузере (локально И на проде после деплоя).
3. **Не заявлять готовым то, что существует только визуально** (Требования §2.14). Кнопка без работающего сервер-экшена — не сделана.
4. **Финансовые вычисления — только в `src/lib/finance.ts`** (сервис с юнит-тестами). В компонентах НИКАКОЙ денежной арифметики, только вызовы сервиса и форматирование.
5. **Деньги — Prisma `Decimal` / Postgres NUMERIC(12,2)**, округление до цента по единому правилу (half-up). JS `number` для денег запрещён в постоянном хранении и расчётах (Требования §2.7).
6. **Миграции — только `prisma migrate`** (переход с `db push` — фаза 1). Перед каждой миграцией схемы: бэкап (`pg_dump` через Neon или экспорт-скрипт). Миграции повторяемы, без orphan records.
7. **Legacy не удалять физически.** Deal, объём двигателя и прочее наследие — soft-выключение и/или legacy-таблицы до ручной проверки (Требования §4, §22).
8. **Права — server-side.** Скрытая кнопка без проверки в Server Action — не защита (Требования §20).
9. **Git:** ветка на фазу → ff-merge в `main` → push (авто-деплой Vercel). Сообщения на русском: `фаза 2: аккаунты и роли`.
10. Каждый новый CSS — внутри `@layer` (см. CLAUDE.md, критично для Tailwind v4).

## Решения, принятые до старта (не пересматривать без пользователя)

| Вопрос | Решение |
|---|---|
| Валюта | EUR, формат `Intl.NumberFormat('de-AT')` → `€ 12.500,00`. Существующие числа НЕ конвертировать по курсу — перечитать как EUR |
| Даты / TZ | `DD.MM.YYYY`, Europe/Vienna |
| Язык UI | Русский; австрийские термины по-немецки: Pickerl, Differenzbesteuerung, Gewährleistung, Kaufvertrag… |
| «Дашборд» | Оставить как есть (Требования используют это слово) |
| Палитра | Убрать доминирующий янтарь. База: Graphit `#3B3F42`, Warm White `#F6F3F2`, White `#FFFFFF`, тёмный фон остаётся. Зелёный/красный/жёлтый/синий — только мелкие функциональные акценты |
| Статусы в БД | Остаются строковыми enum-ами (native enum Postgres не вводить — лишний риск миграций). Валидация через словари `format.ts` + TS-типы |
| Подпись под лого | ⚠️ ждёт решения пользователя (промт: оставить «CRM автосалона»; ранее пользователь просил убрать) |
| Хранилище файлов | ⚠️ ждёт решения пользователя (Vercel Blob или Cloudflare R2) |
| Логотип | ⚠️ ждёт SVG от пользователя или разрешения конвертировать из PDF |

## Фазы

### Фаза 0 — Бренд, валюта, локаль (быстрая, без миграций схемы)
Требования: §3 целиком.
- [ ] MOTORA → MOTORHOF везде (сайдбар, `layout.tsx` metadata, title). Подпись — по решению пользователя
- [ ] Логотип MOTORHOF (SVG) в сайдбар, favicon. Не деформировать
- [ ] Палитра: янтарь → графит/warm-white по таблице выше. Пройти ВСЕ экраны глазами после перекраски (кнопки, чипы, фокус полей, активный пункт меню — читаемость)
- [ ] `fmtMoney` → EUR de-AT; `fmtDate` → DD.MM.YYYY; проверить все места с датами
- [ ] Убрать «MVP v0.1»
- Миграций нет. Данные не трогаются.

Приёмка: скриншоты всех 5 экранов; ни одного `$` в UI; лого не искажён; прод обновлён.

### Фаза 1 — Финансовый фундамент (самая ответственная)
Требования: §2.6–2.8, §10, §12, §24.1.
- [ ] Переход на `prisma migrate` (baseline-миграция текущей схемы)
- [ ] Деньги: Int → `Decimal @db.Decimal(12, 2)` во всех моделях (миграция с сохранением значений)
- [ ] `vitest` в dev-зависимости, `npm test`
- [ ] `src/lib/finance.ts`: Differenzbesteuerung (`differenzUst = max(0, saleGross − einkaufspreis24) × rate/(100+rate)`), Regelbesteuerung (§12.3), маржа до/после расходов, план/факт. ЗАПРЕЩЕНО `(продажа−закупка)×0.2`
- [ ] Юнит-тесты — все кейсы §24.1 дословно (Privat 10000/12000 → USt 333.33, маржа 1666.67; Auktion 10000/10800/12000 → 866.67; продажа ниже §24 → USt 0, убыток виден; округление; отсутствие двойного вычета комиссии)
- [ ] Поля Car: `purchaseChannel`, `taxScheme` (+UNGEKLAERT), `einkaufspreisGemaess24`, `plannedSalePriceGross`, `minimumSalePriceGross`, `arrivalDate`, `currentOwner` (enum §9) — пока просто поля+формы, без условной логики каналов (она в фазе 3)
- [ ] У Expense: `alreadyIncludedInAcquisitionCost`, `deductibleInputVatAmount`, `vatRate/amountNet/amountGross` (§14.1 частично — полная модель в фазе 4)
- [ ] UNGEKLAERT: предупреждение в карточке, суммы не входят в подтверждённые итоги

Приёмка: `vitest run` зелёный; существующие авто/расходы читаются с прежними суммами; маржа на дашборде совпадает с ручным пересчётом по новым формулам (для существующих данных default taxScheme = DIFFERENZBESTEUERUNG, einkaufspreis24 = purchasePrice).

### Фаза 2 — Аккаунты, роли, AuditLog
Требования: §13, §20, §21. (В промте механизм входа не описан — решение ниже согласовано.)
- [ ] Модель User (email, hash пароля, name, role), Auth.js v5 credentials, сессии. Первый ADMIN — сидом из env
- [ ] Basic Auth (`proxy.ts` + APP_PASSWORD) остаётся ВТОРЫМ слоем до конца фазы 3, потом решение пользователя
- [ ] Роли §20: ADMIN, MANAGEMENT, SALES, WORKSHOP, ACCOUNTING, READ_ONLY. Все Server Actions — проверка прав server-side; хелпер `requireRole()`
- [ ] SALES не видит: закупочные цены, einkaufspreis24, полную маржу, внутренние счета (проверить и в UI, и что Server Components не отдают данные)
- [ ] AuditLog (§21): entityType, entityId, action, before/after (JSON), userId, timestamp, reason. Писать из всех мутаций перечисленных сущностей
- [ ] Mindestverkaufspreis: продажа/бронь ниже минимума — блок + manager override с комментарием + AuditLog
- [ ] Тесты §24.5

### Фаза 3 — Автомобили глубоко
Требования: §5.3, §6, §7, §8, §9, §11, §18.
- [ ] 8 статусов (§6.1) + миграция старых (§22: Подготовка→IN_PREPARATION, В наличии→READY_FOR_SALE, Бронь→RESERVED, Продан→SOLD)
- [ ] Парковка §7: systemSequence (MH-код, неизменяемый), parkingRow/Spot, уникальность активного места, история перемещений, отображение `A-12 / MH-0042`
- [ ] Форма авто по секциям (§8): основные данные, Serviceheft (enum), Nachlackierungen (+части кузова структурно), Pickerl (+modal создания задачи, без дублей, §8.4), убрать объём двигателя из UI (поле в БД оставить, задокументировать legacy)
- [ ] Фото и документы (§8.5): выбранное хранилище, галерея, checklist обязательных документов (настраиваемый), PDF/JPG/PNG/WEBP
- [ ] Владельцы §9: условный блок для Mriya/A Motors/AutoHub, внутренний счёт e.U.→OG, раздельные результаты компаний, продажа партнёрского авто требует внутренний счёт или статус «ожидает»
- [ ] Условные поля закупки по каналам §11 (Privat/Auktion/Händler/Inzahlungnahme/Import) + проверки (auctionTotal ≥ Fahrzeugpreis без override и т.д.)
- [ ] Бронь и продажа внутри авто §18: обязательные поля, financial snapshot при продаже, освобождение парковки, запрет правок финансов задним числом без override
- [ ] Sale-сущность; миграция завершённых Deal → Sale; незавершённые Deal → legacy-таблица (§22)
- [ ] Таблица авто §6.2: новые колонки (по ролям!), фильтры, поиск по VIN/коду/парковке
- [ ] Тесты §24.2 (Pickerl), §24.3 (парковка)

### Фаза 4 — Расходы, задачи, календарь
Требования: §14, §15, §16.
- [ ] Страница «Расходы» (полная модель §14.1, категории §14.2, фильтры, итоги net/USt/gross, экспорт CSV, привязка к авто)
- [ ] Задачи §15: type/assignedTo/priority/status/attachments, CANCELLED вместо удаления, фильтры, быстрая форма в карточке авто
- [ ] Календарь §16: Appointment, представления месяц/неделя/день/список, конфликт-чек server-side (сотрудник, авто, соседние интервалы не конфликтуют, отменённые не блокируют), Europe/Vienna
- [ ] Тесты §24.4

### Фаза 5 — Gewährleistung, новый дашборд, снос Deal UI, финал
Требования: §4, §5, §17, §19, §22–§25.
- [ ] Gewährleistungsfälle §19 (сущность, страница, статусы, связи с задачами/расходами)
- [ ] Клиенты §17: убрать зависимость от Deal, связи с авто/терминами/бронями, источники: Willhaben, AutoScout24, Website, Empfehlung, Laufkundschaft, Social Media, Sonstiges
- [ ] Новая навигация §4 (8 пунктов, «Настройки» по ролям), полное удаление Deal UI
- [ ] Дашборд §5: KPI (клик → фильтрованный список), строка статус-чипов, просмотры+задачи, «Требует внимания» (Pickerl, без фото, без документов, 30/60/90 дней, открытые Gewährleistungsfälle)
- [ ] UX-проход §23: пагинация, фильтры в URL, loading/empty/error, подтверждения destructive, «несохранённые изменения»
- [ ] Тесты §24.6; сид с австрийскими обезличенными данными
- [ ] Прогон полного Definition of Done (§25) + финальный отчёт §26

## Открытые вопросы (блокеры старта соответствующих фаз)

1. Подпись «CRM автосалона» под лого — убрать или оставить? → фаза 0
2. SVG логотипа или конвертация из PDF? → фаза 0
3. На проде реальные данные салона или ещё тестовые? (можно ли пересоздавать базу свободно) → фаза 1
4. Хранилище: Vercel Blob или Cloudflare R2? → фаза 3
5. Список сотрудников (имена/email/роли) для заведения аккаунтов → фаза 2
6. Налоговые формулы после реализации показать Steuerberater'у заказчика (Требования §1) — зафиксировать, что CRM не заменяет бухгалтерию

## Прогресс

- [ ] Фаза 0
- [ ] Фаза 1
- [ ] Фаза 2
- [ ] Фаза 3
- [ ] Фаза 4
- [ ] Фаза 5
